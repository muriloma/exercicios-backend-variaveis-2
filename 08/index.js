// comprimento e area
let raio = 1;
let area = Math.PI * (raio ** 2), comprimento = 2 * Math.PI * raio;
comprimento = comprimento.toFixed(2), area = area.toFixed(2);
console.log(`Para um raio de ${raio} o comprimento é ${comprimento} e a área ${area}`);